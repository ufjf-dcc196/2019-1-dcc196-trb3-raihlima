<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trabalho3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ProducaoActivity"></activity>
        <activity android:name=".activity.GerenciarProducaoActivity" />
        <activity android:name=".activity.GerenciarCategoriaActivity" />
        <activity android:name=".activity.CandidatoActivity" />
        <activity android:name=".activity.GerenciarCandidatoActivity" />
        <activity android:name=".activity.AtividadeActivity" />
        <activity android:name=".activity.CadastrarProducaoActivity" />
        <activity android:name=".activity.CadastrarCandidatoActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>